# iii 引擎 WebSocket 地址（浏览器 RBAC 监听端口，默认 3110）
VITE_ENGINE_URL=ws://localhost:3110

# 浏览器 Worker 注册名
VITE_WORKER_NAME=docx-audit-ui

# 默认项目
VITE_DEFAULT_PROJECT=M1212

# 默认项目数据根目录（仅用于拼接显示路径，文件需通过引擎侧访问）
VITE_DATA_ROOT=.
